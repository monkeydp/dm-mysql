node:
  structure:
    conn:
      db:
        tables:
          table:
        views:
          view:
# start parsing when the prop named "menu" is found (ignore the root "menu")
# if menu item has no prop named "instr", recognize key as "action" and value as "target"
menu:
  structure:
    node:
      conn:
        menu:
          items:
            - open: conn
            - close: conn
            - instr:
                action: manage
                target: group
              menu:
                items:
                  - new: group
      db:
        menu:
          items:
            - open: db
      tables:
      table:
      views:
      view: